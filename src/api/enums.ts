export enum HttpMethod {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  PATCH = 'patch',
  DELETE = 'delete',
}

export enum TextType {
  WORD = 'word',
  SENTENCE = 'sentence',
}

export enum SupabaseErrorCode {
  DUPLICATE = '23505',
}
